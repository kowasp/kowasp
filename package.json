{
  "name": "kowasp",
  "version": "1.0.0",
  "description": "KOWASP Security Scanner monorepo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:all": "concurrently \"npm --prefix kowasp-backend test\" \"npm --prefix kowasp-core test\" \"npm --prefix kowasp-frontend test\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 